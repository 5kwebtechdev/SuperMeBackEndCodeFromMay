#name: CI/CD Pipeline
## trigger-fix: 2026-05-26
#
#on:
#  push:
#    branches:
#      - main
#      - dev-branch
#  pull_request:
#    branches:
#      - main
#
#env:
#  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
#
#jobs:
#  # ─── Job 1: Build & Test (runs on every push) ─────────────────────────────
#  build:
#    name: Build & Test
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Set up Java 21
#        uses: actions/setup-java@v4
#        with:
#          java-version: '21'
#          distribution: 'temurin'
#          cache: maven
#
#      - name: Build with Maven (skip tests for now)
#        run: mvn clean package -DskipTests
#
#      - name: Upload JAR artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: superme-jar
#          path: target/superme-0.0.1-SNAPSHOT.jar
#          retention-days: 1
#
#  # ─── Job 2: Deploy (only runs when push is to main) ───────────────────────
#  deploy:
#    name: Deploy to Production
#    runs-on: ubuntu-latest
#    needs: build
#    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev-branch') && github.event_name == 'push'
#
#    steps:
#      - name: Download JAR artifact
#        uses: actions/download-artifact@v4
#        with:
#          name: superme-jar
#
#      - name: Set up SSH key
#        run: |
#          mkdir -p ~/.ssh
#          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/id_rsa
#          chmod 600 ~/.ssh/id_rsa
#          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts
#
#      - name: Upload JAR to server
#        run: |
#          scp -o StrictHostKeyChecking=no \
#            superme-0.0.1-SNAPSHOT.jar \
#            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/var/www/superMeBackend/superme-0.0.1-SNAPSHOT.jar
#
#      - name: Restart application on server
#        run: |
#          ssh -o StrictHostKeyChecking=no \
#            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
#            "systemctl restart superme && systemctl status superme --no-pager"